# Models Directory

Saved PCA/SVD components and models will be stored here.

Use this directory to save and load decomposition results for reuse.

The files in this directory are gitignored by default.
